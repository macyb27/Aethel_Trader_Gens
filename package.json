{
  "name": "aether-trader",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "dev": "pnpm --filter aether-trader-omega dev",
    "dev:web": "pnpm --filter aether-trader-omega dev",
    "dev:mobile": "pnpm --filter app-template dev",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.5.4"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
